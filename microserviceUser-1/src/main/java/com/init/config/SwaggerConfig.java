package com.init.config;

/*import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import springfox.ducumentation.spring.web.plugins.Docket;
import springfox.ducumentation.swagger2.annotations.EnableSwagger2;
import springfox.ducumentation.spi.DocumentationType;
import springfox.ducumentation.builders.RequestHandlerSelectors;
import springfox.ducumentation.builders.PathSelectors;

@Configuration
@EnableSwagger2*/
public class SwaggerConfig {
    
   /* @Bean
    public Docket api(){
        
        return new Docket(DocumentationType.SWAGGER_2)
                .select()
                .apis(RequestHandlerSelectors.any())
                .paths(PathSelectors.any())
                .build();
        
    }*/
    
}